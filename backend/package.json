{
  "name": "banking-mock-api",
  "version": "1.0.0",
  "description": "Mock API for Mini Banking Dashboard take-home",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
